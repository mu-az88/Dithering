Place your image files in this folder for the dithering script.

PNG or JPG formats are preferred

After adding an image, update line 79 in `Dithering code.py` to point to the chosen image (for example: `images/your_image.png`) and test the code on your own image.