Place your image files here for the dithering script.

Naming suggestions:
- Use simple names without spaces: example.png
- Prefer PNG or JPG formats: example.png or example.jpg

After adding an image, update line 79 in `Dithering code.py` to point to the chosen file (for example: `images/your_image.png`) and commit the file to the repo.